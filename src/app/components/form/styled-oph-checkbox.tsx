import { styled } from '@mui/material';
import { OphCheckbox, ophColors } from '@opetushallitus/oph-design-system';

export const StyledOphCheckBox = styled(OphCheckbox)(() => ({
  backgroundColor: ophColors.white,
}));
