# Lokaalin kehityksen konfiguraatio (mvn spring-boot:run)
server:
  port: 8080
  servlet:
    context-path: /valintojen-toteuttaminen
host:
  virkailija: virkailija.untuvaopintopolku.fi
management:
  endpoints:
    web:
      exposure:
        include: health
